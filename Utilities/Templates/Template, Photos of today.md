
```photos
{
  "filters": {
    "dateFilter": {
      "ranges": [
        {
          "startDate": {
            "year": <%tp.date.weekday("YYYY", 0)%>,
            "month": <%tp.date.weekday("M", 0)%>,
            "day": <%tp.date.weekday("D", 0)%>
          },
          "endDate": {
            "year": <%tp.date.weekday("YYYY", 0)%>,
            "month": <%tp.date.weekday("M", 0)%>,
            "day": <%tp.date.weekday("D", 0)%>
          }
        }
      ]
    }
  }
}
```